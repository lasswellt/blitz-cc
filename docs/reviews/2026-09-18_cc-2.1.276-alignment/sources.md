# Sources

All fetched 2026-09-18 unless noted. Version numbers in the review trace to these rows.

| # | Source | What was taken |
|---|---|---|
| 1 | https://code.claude.com/docs/en/best-practices | Verification ladder (`/goal`, Stop hook with 8-block cap, verification subagent, dynamic workflow), `/verify`, explore/plan/code, CLAUDE.md include/exclude table and `/doctor` trims, `/batch` (5–30 subagents in worktrees), `/btw`, Writer/Reviewer sessions, auto mode, agent view research preview, agent teams experimental. |
| 2 | https://claude.com/blog/maximizing-the-value-of-your-claude-code-sessions | Set model + effort once (switching busts prompt cache); `/context`; `/compact` before a break; `/rewind` vs `/compact`; `/rename` before `/clear`; quiet flags in CLAUDE.md; 30 k-char output auto-file; `/loop` in own session; subagents on haiku/sonnet for noisy jobs; `/autocompact 200k` on 1M models (2.1.221+); cache economics (1 h subscription / 5 min API). |
| 3 | https://claude.com/blog/agentic-coding-is-straining-ci-heres-how-we-scaled-test-impact-analysis-at-anthropic | Listener / selector architecture; 25× CI growth in six months; three failed patches; stateless workers + append-only journal + consumer roll-up; recommendations: plan for 10–20× in v0, instrument for AI observation (in == out), no singleton bottlenecks. |
| 4 | https://claude.com/blog/projects-redesigned | Threads as independent Claude Code cloud sessions on own branches; coordinator; parallel execution with merge-conflict resolution; persistent project memory; check-in frequency and thread-creation settings; beta from 2026-09-17; local execution "coming soon". |
| 5 | https://www.stack-junkie.com/blog/building-a-dashboard-for-your-ai-agent (proxy-blocked; text pasted by the user) | Lantern: Tasks / Inbox / Log JSON entities, REST API, heartbeat pattern (hourly; escalate blocked > 24 h and stale inbox > 7 d; `HEARTBEAT_OK`), "make dashboard updates non-optional", JSON until concurrency forces SQLite, design for the agent first. Published 2026-02-13. |
| 6 | https://github.com/herdrdev/herdr (herdr.dev proxy-blocked) | Rust terminal runtime for agents; sidebar states working / blocked / idle / needs-input; background server persists sessions; agents spawn panes and "wait until another agent is genuinely blocked"; socket API; multi-machine; Apache-2.0. |
| 7 | https://github.com/umutciloglu/herdr-session-manager | SQLite index of every Claude Code / Codex session; cross-harness mailbox; delivery immediately when idle or at turn end via Stop hook; address `<harness>:<session id>`. |
| 8 | https://code.claude.com/docs/en/changelog | Releases 2.1.265–2.1.276 in detail (plugin eval 2.1.269; `omitClaudeMd` 2.1.271; Monitor `persistent` removed 2.1.271; `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` 2.1.269; `maxEffortLevel` 2.1.267; `syncClaudeAiSkills` 2.1.275; `/output-style`, `/focus`; OTel additions). Earlier releases summarized by feature. |
| 9 | https://code.claude.com/docs/en/hooks | Full event list (33), hook types command/http/mcp_tool/prompt/agent, matcher semantics, fields `if`/`timeout`/`statusMessage`/`once`/`async`/`asyncRewake`/`args`, common input fields incl. `scratchpad_dir` (2.1.257+) and `effort` (2.1.268+), exit-code and JSON output contract. |
| 10 | https://code.claude.com/docs/en/goal | Prompt-based Stop hook on the small fast model; verdicts met / not yet / impossible; no-progress stop; check-ins every 30 min during background work (2.1.234+), idle check-ins (2.1.236+), retry/pause (2.1.269+); `-p` support; restored on resume. |
| 11 | https://code.claude.com/docs/en/memory | CLAUDE.md scopes, ≤ 200-line target, `@import` (4 hops), HTML comments stripped, `.claude/rules/` with `paths:`, `claudeMdExcludes`; auto memory typed files (user/feedback/project/reference), `MEMORY.md` 200 lines / 25 KB, `autoMemoryEnabled`, `autoMemoryDirectory`, `modified` frontmatter (2.1.214+); `/memory`; `InstructionsLoaded` hook. |
| 12 | https://code.claude.com/docs/en/agent-view | `claude agents` (2.1.140+), `--json [--all]` schema (`state`, `status`, `waitingFor`, `pid`, `sessionId`, `name`, `cwd`, `kind`), dispatch syntax, `--bg`, `/bg`, `/fork`, attach/logs/stop/respawn/rm, supervisor daemon, worktree isolation `worktree.bgIsolation`. |
| 13 | https://code.claude.com/docs/en/cross-session-messaging | `ListAgents` / `SendMessage` (2.1.224+; Windows 2.1.234+), `notify_when_idle` (2.1.236+), `@session` mentions (2.1.232+), `crossSessionInbound` accept/hold/refuse, `isolatePeerMachines`, `dialogExpiry`, inbox socket + `CLAUDE_CODE_MESSAGING_SOCKET` / `CLAUDE_CODE_MESSAGING_TOKEN` exported to hooks, own-child verification, same-container constraint, rate limits. |
| 14 | https://code.claude.com/docs/en/sub-agents | Frontmatter fields incl. `memory` user/project/local, `background`, `isolation: worktree`, `omitClaudeMd` (2.1.271+), `skills`, `experimental.cacheTtl` (2.1.248+), `maxTurns` partial (2.1.246+), `model: inherit` (2.1.198+); plugin-prohibited fields; what loads at startup; resume via `SendMessage`; fork semantics; `CLAUDE_CODE_SUBAGENT_MODEL(_FORCE)`; tools removed from subagents. |
| 15 | https://code.claude.com/docs/en/skills | Frontmatter fields incl. `when_to_use`, `arguments`, `context: fork` + `agent` + `background`, `hooks`, `paths`, `user-invocable`, `shell`; substitutions `${CLAUDE_SESSION_ID}`, `${CLAUDE_EFFORT}`, `${CLAUDE_SKILL_DIR}`; bundled skills; `/skill-doctor` (2.1.252+); `/verify` recipe recording (2.1.200+); compaction budget 25 k tokens. |
| 16 | https://code.claude.com/docs/en/plugins-reference | Manifest fields, `experimental.evals` path, eval layout and JSON/HTML reports, `monitors.json`, plugin hook events, `settings.json` `agent` key, lockfile install policy. |
| 17 | https://code.claude.com/docs/en/workflows | Workflow script API (`agent`, `pipeline`, `parallel`, `phase`, `log`, `args`), `Date.now()`/`Math.random()` throw, 16 concurrent default, `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (2.1.269+), `workflowSizeGuideline` (2.1.219+), plugin `workflows/` distribution, resume replay, usage-limit pause (2.1.271+), `/deep-research`. |
| 18 | https://code.claude.com/docs/en/scheduled-tasks | `/loop` self-paced via `ScheduleWakeup`, fixed-interval CronCreate, `.claude/loop.md`, jitter, 7-day expiry, not restored on resume for self-paced loops, comparison with Routines (1 h min) and Desktop tasks, Monitor as alternative to polling. |
| 19 | https://code.claude.com/docs/en/channels | Research preview; MCP channel plugins push CI / chat events into a running session; `--channels`; `channelsEnabled` / `allowedChannelPlugins`; sender allowlists; permission relay. |
| 20 | https://code.claude.com/docs/en/sessions | `--continue` / `--resume <name|id|path>` / `--from-pr`, `/rename`, `--name`, default display names (2.1.196+), `/branch`, transcript location `~/.claude/projects/<project>/<session-id>.jsonl` (format internal, changes between versions), `transcript_path` for hooks, `/export`, `cleanupPeriodDays`. |
| 21 | https://code.claude.com/docs/en/context-window | Startup load order (system prompt, auto memory, CLAUDE.md, rules, deferred MCP tools), what survives compaction, `/context`. |

Not reached: https://dotzlaw.com/insights/claude-code-13-herdr-parallel-agent-sessions/ (proxy-blocked; secondary to sources 6–7).
